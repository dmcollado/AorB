require 'rails_helper'
# A backend test(?): from controller not to allow create a poll instance w/o having 2 poll items?
describe "In poll the create page", :type => :feature do

  # it "should crate a poll when 2 poll items" do
  # poll = Poll.create
  # end

end
